# Environment Dependencies

Refer to https://www.open-mpi.org/software/ompi/v4.0/
```shell
tar -zxvf openmpi-4.0.7.tar.gz
cd openmpi-4.0.7
./configure
make -j$(nproc)
sudo make install
sudo ldconfig
```

